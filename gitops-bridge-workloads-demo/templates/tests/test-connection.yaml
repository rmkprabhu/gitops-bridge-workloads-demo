apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitops-bridge-workloads-demo.fullname" . }}-test-connection"
  labels:
    {{- include "gitops-bridge-workloads-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitops-bridge-workloads-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
